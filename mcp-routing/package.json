{
  "name": "fleetops-routing-mcp",
  "version": "1.0.0",
  "private": true,
  "description": "FleetOps routing MCP server — exposes deterministic multi-stop route optimization (OSM/OSRM + nearest-neighbor) and geocoding as MCP tools so an agent can reason over routes without hallucinating distances.",
  "type": "module",
  "bin": {
    "fleetops-routing-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.11.5",
    "tsx": "^4.7.0",
    "typescript": "^5.6.3"
  }
}
